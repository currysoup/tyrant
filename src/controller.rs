use math::Vector3;

// Obviously not really controlling anything right now, just applying a constant velocity. This
// will be totally refactored to work with player input.
pub fn process() -> Vector3 {
    let mut vel = Vector3::zero();

    if true {
        vel.x = 1.0;
    }

    vel
}
